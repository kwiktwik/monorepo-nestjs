{
  "name": "client-events-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute client-events --file=src/schema.sql",
    "db:init:local": "wrangler d1 execute client-events --local --file=src/schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250514.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.14.0"
  }
}